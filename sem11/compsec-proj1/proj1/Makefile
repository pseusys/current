submission:
	tar cvzf submission.tar.gz \
	  --exclude-backups --exclude='xploit[1-5]' --exclude=run-shellcode \
	  --exclude='*.o' --exclude='*.bin' --ignore-failed-read \
	  xploits/ ID.csv fuzz/README fuzz/results/fuzzer_stats fuzz/results/crashes/
